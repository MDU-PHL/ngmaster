[metadata]
license_file = LICENSE

[tool:pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    integration: marks tests that require mlst in PATH (deselect with -m "not integration")
    slow: marks tests as slow-running